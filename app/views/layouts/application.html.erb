<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <%= csrf_meta_tag %>
    
    <!-- this is the partial to the _stylesheet -->
    <%= render 'layouts/stylesheets' %>
    	
  </head>
  <body>
  	<div class="container">
  	  <%= render 'layouts/header' %>	
  	  <section class="round">
  	    <%= yield %>		
  	  </section>
  	  <%= render 'layouts/footer'%>
  	  <%= debug(params) if Rails.env.development? %>
  	</div>
  </body>
</html>